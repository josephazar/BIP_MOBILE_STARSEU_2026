<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 🚨 VULNERABILITY #11: Excessive Permissions -->
    <!-- OWASP: M6 - Inadequate Privacy Controls -->
    <!-- Risk: Requesting unnecessary permissions violates user privacy -->
    <!-- Solution: Only request permissions that are actually needed -->
    <!-- Reference: https://owasp.org/www-project-mobile-top-10/2023-risks/ -->
    
    <!-- VULNERABLE: These permissions are NOT needed for a note-taking app -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Actually needed permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- 🚨 VULNERABILITY #12: Debug Mode in Production -->
    <!-- OWASP: M8 - Security Misconfiguration -->
    <!-- Risk: App can be easily debugged and reverse-engineered -->
    <!-- Solution: Set android:debuggable="false" for production builds -->
    <!-- Reference: https://owasp.org/www-project-mobile-top-10/2023-risks/ -->

    <application
        android:label="VulnNote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:debuggable="true"
        android:allowBackup="true"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
